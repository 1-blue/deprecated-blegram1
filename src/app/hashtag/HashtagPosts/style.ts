import styled from "styled-components";

import StyledPost from "@src/components/Post/style";

/** 2023/05/06 - 특정 해시태그에 해당하는 게시글들 렌더링 컴포넌트 스타일 - by 1-blue */
const StyledHashtagPosts = styled(StyledPost)``;

export default StyledHashtagPosts;
